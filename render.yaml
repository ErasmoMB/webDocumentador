services:
  - type: web
    name: frontend-lbs
    env: node
    buildCommand: npm ci && npm run build:prod
    startCommand: npx serve dist/lbs -s -l $PORT
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: NODE_ENV
        value: production
      - key: API_URL
        value: "https://backend-api-lbs.onrender.com"
      - key: USE_MOCK_DATA
        value: "false"
